<div class="container" align="center">
  <h1><%= @piece.piece_name %></h1>
    <%= image_tag @piece.photo.url(:large)%>
  <br />
  <br />
  <%= button_to "Add a Comment", new_piece_comment_path(@piece), :method => :get %>
   

  <% @comments.each do |comment| %>
  <%= comment.body %>
  <br>
  <%= comment.artist.name %>
  <br>
  <% end %>
</div>